# Ceph
Ceph is a distributed storage system that provides block, file, and object storage. It stores data across multiple nodes using OSDs, while MON maintains cluster state. MGR provides monitoring, MDS handles file metadata, and RGW enables S3 access, ensuring scalability and high availability.
